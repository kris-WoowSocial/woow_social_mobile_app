import 'package:flutter/material.dart';

class HomeTabFollowing extends StatelessWidget {
  const HomeTabFollowing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
