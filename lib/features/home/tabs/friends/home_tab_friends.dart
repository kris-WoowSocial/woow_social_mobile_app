import 'package:flutter/material.dart';

class HomeTabFriends extends StatelessWidget {
  const HomeTabFriends({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
